# This file managed by Chef

# Disabling this is generally a bad idea, but it makes
# using Disco a whole lot easier (we avoid having to
# generate a known_hosts file correctly)
StrictHostKeyChecking no
